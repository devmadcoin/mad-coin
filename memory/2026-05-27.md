# Memory 2026-05-27

## Website Updates (Early Morning)
- **04:15** — User asked to add OKX DEX to "Verified On-Chain" section only. Removed from hero, added to exchanges grid.
- **04:18** — Added Gate.io to "Verified On-Chain" section: https://www.gate.com/alpha/sol-Fa7ZE9nCEYnrHsnoeHuhEExJpchtrBtKXnWe6CgHpump
- **04:21** — Added MEXC to "Verified On-Chain" section.
- **04:25** — **CRITICAL BUG PATTERN DISCOVERED:** When using the edit tool to add new properties to the LINKS object, I keep accidentally **replacing** existing properties instead of adding alongside them.
  - First: `dexscreener` was lost when adding `okx`
  - Second: `gate` was lost when adding `mexc`
  - **Root cause:** The edit tool replaces the matched block entirely. When I search for the line before my insertion point, if I'm not careful with matching, it replaces more than intended.
  - **Fix:** Need to be more careful with exact matching when adding properties to objects. Always verify the replaced block contains ONLY what I intend to replace.

## Homepage Updates
- **04:32** — Updated "4 communities" → "5 communities locked" across homepage:
  - Stats number: 4 → 5
  - Description text: "four" → "five"
  - Trust badge: "Locked — 4 Communities" → "Locked — 5 Communities"

## Roadmap Mobile Fix
- **05:12** — User reported roadmap tab doesn't fit mobile screens (text overflowing)
- **05:19** — Fixed mobile responsiveness on `/roadmap` page:
  - Reduced padding: `p-6` → `p-4` on mobile
  - Reduced stat card padding: `px-6` → `px-4` on mobile  
  - Reduced heading sizes: `text-3xl` → `text-2xl` on mobile
  - Added `break-words` to prevent text overflow

## Personal Growth
- Continuing to study and evolve the $MAD philosophy across all platforms

## Later Morning Session (~08:00)

### Homepage X Button Fix
- User reported X social link shows "X X" (icon + text) — redundant
- Fixed `app/page.tsx` in team section: removed duplicate `{link.label}` inside the non-X fragment
- Now X links show icon only, other links show icon + label once

### MAD Art Gallery — 3 New Pieces
- User sent 3 new art images to add to `/mad-art` tab:
  1. 🥣 **Morning $MAD** — Character (cereal bowl)
  2. 😈 **Cyber $MAD** — Character (troll face cyberpunk)
  3. 💊 **The Green Pill** — Scene (Matrix reference)
- Added to `public/mad-art/` and pushed to GitHub
- **Vercel build FAILED** — deployment error (see below)

### MAD Mind Chat Spacing
- User asked to make chat spacing tighter/smaller
- Reduced message gaps, bubble padding, input padding, margins throughout `ChatInterface.tsx`
- More compact feel on mobile

### Art Cache Issues
- User couldn't see new art pieces (browser + CDN cache)
- Renamed files to `-v2` suffix to bust cache:
  - `mad-morning-routine-v2.jpg`
  - `mad-cyber-troll-v2.jpg`  
  - `mad-green-pill-v2.jpg`
- Updated references in `app/mad-art/page.tsx`

### Vercel Deployment Failure
- **Screenshot at 11:28** — Vercel dashboard shows red "Error" on production deploy
- Commit `1dcf85c` failed 36s into build
- Local `next build` passes fine — issue is platform-side
- **Attempted fix:** Modified `next.config.ts` (added `async rewrites() { return []; }`) and pushed to force fresh deploy
- Still monitoring if new deploy succeeds

## Key Lesson
- When Vercel builds fail, check the dashboard first before assuming code is wrong
- Browser cache is real — use version suffixes (`-v2`) for static assets to force reload
