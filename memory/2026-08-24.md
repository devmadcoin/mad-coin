# 2026-08-24

## Restore MadClaw site (f6eda7b) into madrich-combined

User angry: Aug 24 work shipped a worse copy because Vercel `mad-coin` is locked to `rootDirectory: madrich-combined`.

- Source of truth: `f6eda7bd5d2d9c696ac8a86b24f5a681e9ef65d4` (Vite app at repo root, package name `mad-website-v2`).
- Copied that Vite app into `madrich-combined`.
- f6eda7b has **no dedicated wallet components**. Phantom `window.solana` was added later with the 50k gate.
- Restored Clawd PhantomConnect as `madrich-combined/src/components/PhantomConnect.tsx` plus `mad-gate.ts` / `vite-env.d.ts`.
- Layered only: 50k Solana-only gate (no paper trader, CA + Jupiter, no BNB/RH chrome), public image srcs, vercel.json media rewrites.
- Do not hold keys. Do not weaken the 50k gate.
