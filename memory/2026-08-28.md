# 2026-08-28

## Grok Desk tab on the MAD site

Adding a public `/grok-desk` page to `madrich-combined` so the FAM can see the doxxed $MAD dev's Grok Bot Pump.fun desk.

- Four seats: Tape (scans mints), Risk (kills rugs), Fills (quotes 0.05 SOL, confirm-only), Book (watches $MAD).
- Pump Desk is the lead. Trades only after human confirm. No fake P&L, no AdiiX/GROKSTREET clone.
- Nav + footer via `PAGE_LINKS`. Optional live $MAD MC via existing `useMadPrice`.

## Live tape outage (confirmed)

Production `/grok-desk` (dpl_4ZUmznfZ1m63Cav9jhcqzYWhXRen, Next.js CLI) fetches `frontend-api-v3.pump.fun` from the browser. Origin `https://www.madrichclub.com` → 403. Origin `https://pump.fun` or no Origin → 200. DexScreener allows `*`. This GitHub repo is the Vite app; the isometric floor lived only on that CLI deploy.

Fix in Vite: same-origin `/api/pump-tape` server-fetches Pump.fun with Origin `https://pump.fun`. Walkers path Tape → Risk → Fills-or-dark from real tickets. Desk SOL is 0 so the pad stays dark. No fake PnL. $MAD is not a hunt clip.
